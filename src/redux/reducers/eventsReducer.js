const initialState = {
    enabledSources: {},
    enabledUsers: {}
}

const eventsReducer = ( state = {}, action ) => {
    switch (action.type) {
        default:
            return initialState;
    }
}

export default eventsReducer;